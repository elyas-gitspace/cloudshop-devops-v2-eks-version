  apiVersion: v1
  kind: Service
  metadata:
    name: frontend-service
  spec:
    type: LoadBalancer  # Crée un Load Balancer AWS pour exposer le frontend sur Internet
    selector:
      app: frontend     # Cible les pods avec le label `app: frontend`
    ports:
      - protocol: TCP
        port: 80         # Port exposé par le Load Balancer (HTTP)
        targetPort: 5000 # Port des pods (ton application Flask écoute sur 5000)
  ---
  #ici, à la demande de EKS (kubernetes) AWS crée un Load Balancer qui écoute sur le port 80 et redirige vers le port 5000 de mes pods frontend
  #┌──────────────────────────────────────────────────────────────────────────────
  #│                                                                               
  #│   🌍 Utilisateur                                                               
  #│      │                                                                       
  #│      ▼ (Requête HTTP sur port 80)                                             
  #│   🚪 [Load Balancer AWS]  ← Créé automatiquement par Kubernetes              
  #│      │                                                                       
  #│      ▼ (Redirection vers port 5000)                                          
  #│   🏭 [Pods Frontend]  ← 2 réplicas (conteneurs Flask)                         
  #│                                                                               
  #└──────────────────────────────────────────────────────────────────────────────
